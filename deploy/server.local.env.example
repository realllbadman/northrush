# Copy to server.local.env and fill in. That file is gitignored: the server
# address is deliberately kept out of a public repo.
#
#   cp deploy/server.local.env.example deploy/server.local.env
#   source deploy/server.local.env
#
SERVER_IP=203.0.113.10
DOMAIN=example.com
